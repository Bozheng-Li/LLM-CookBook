# 《大模型技术 Cookbook》目录重构方案 v0.2

> 审计日期：2026-08-07　｜　审计对象：`tools/toc.json` v0.1 骨架版（11 篇 / 62 条）
> 产出：`tools/toc.proposed.json`（16 篇 / 98 条）+ `tools/migrate_v02.py`（迁移脚本）

---

## 一、一句话结论

现有目录的**分层骨架是好的**，问题不在"排得乱"，而在**七个知识层整层缺失**——其中三层（GPU 与算子系统、工程化运维、数据合规）在 62 条里一个字都没出现。补齐后从 62 条扩到 98 条，但配套引入分批机制，v1.0 实际写作量控制在约 84 条。

---

## 二、现状诊断

### 2.1 规模与完成度

| 指标 | 现状 |
|---|---|
| 结构 | 11 篇 / 62 词条 / 73 页 |
| 难度分布 | 入门 16 (26%) / 进阶 30 (48%) / 高级 16 (26%) — **梯度设计合理** |
| 阶段划分 | 四段：基础层 14 / 架构核心 13 / 训练与对齐 13 / **推理与应用 22** |
| **内容完成度** | **3 / 73 页（4%）** — 仅 `attention`、`moe`、`rag` 为精写页，其余 70 页是 3.4KB 占位骨架 |
| 工程底座 | `toc.json` 单一数据源 → `generate.py` 一键重生成全站；精编页有 `<!-- cookbook:handcrafted -->` 保护 |

**工程底座是这次重构成本极低的原因**：目录调整只需改 JSON + 跑脚本。

### 2.2 关键词缺口探测

对 62 条的全部 `title / desc / keywords` 做关键词扫描，以下 2026 年高频概念**完全查无此词**：

```
flashattention · triton · kernel · cuda · model merging · continual pretraining
世界模型 · 具身 · A2A · SAE · 可解释性 · circuit · computer use · GUI agent
扩散语言模型 · 水印 · 版权 · provenance · 端侧 · SLM · PD 分离 · expert parallel 部署
混合推理 · 代码模型 · 可观测性 · trace · canary · p95 · 版本管理 · 护栏 · LLMOps
```

### 2.3 七个整层缺失

| 缺失层 | 原覆盖 | 严重度 |
|---|---|---|
| GPU 与算子系统（FlashAttention / Triton / kernel） | 0 条 | 🔴 深度断层 |
| 工程化与运维（可观测性 / 评估门禁 / 成本治理） | 0 条 | 🔴 从 demo 到线上那一跃 |
| 数据合规（版权 / 水印 / 溯源） | 0 条 | 🔴 2026-08 已是硬约束 |
| 可解释性（SAE / 电路分析） | 0 条 | 🟠 |
| 动手实践 | 0 条 | 🟠 Cookbook 却没有"菜谱" |
| Agent 工程 | 1 条 | 🟠 Berkeley 为此单开一门课 |
| 推理与部署深水区 | 部分 | 🟠 有 vLLM，没有 PD 分离 / 端侧 / 混合推理 |

### 2.4 其他结构性问题

- **颗粒度失衡**：`distributed-training` 一条要覆盖 5 种并行 + 3 个框架（CS336 用整个模块讲）；`agent` 一条塞下 ReAct + 规划 + 记忆 + 多智能体
- **冗余**：蒸馏被拆成 `distillation-post`(P06) 与 `distillation-deploy`(P08) 两条
- **权重失当**：Part 02 里 RNN/LSTM + Seq2Seq 占 40%，2026 年这两条的教学价值只剩"注意力为什么被发明"
- **分级矛盾**：`tokenization` 标 inter 而 `attention` 标 basic — BPE 合并规则不可能比缩放点积注意力更难
- **事实错误**：`governance` 混淆了 2023 年《暂行办法》（部门规范性文件）与 GB 45438-2025（强制性国标）

---

## 三、调研依据

对照六个权威知识体系（共 36 条来源，其中一手来源含高校课程官网、厂商公告、法规原文、arXiv 论文）：

| 体系 | 提供的对照价值 |
|---|---|
| [Stanford CS336](https://cs336.stanford.edu/) | 5 大模块，其中 **Systems 模块含用 Triton 手写 FlashAttention-2 的作业** |
| [CMU 11-711 Fall 2025](https://2025.cmu-llms.org/schedule/) | 把评估放第 4 周；tool use / RAG / **可解释性** / 偏见伦理列为独立基础模块 |
| [Berkeley CS294 Agentic AI](https://rdi.berkeley.edu/agentic-ai/f25) | Agent 独立开课：评估、可验证后训练、多智能体、具身、Agent 安全 |
| [Hugging Face LLM Course](https://huggingface.co/learn/llm-course) | 实践导向 + 本地部署 |
| [mlabonne/llm-course](https://github.com/mlabonne/llm-course) | 动手 track：LLM 基础 / RAG / fine-tuning |
| [赵鑫《大语言模型》](https://llmbook-zh.github.io/) | 中文权威 4 部 13 章体系 |

**时效锚点（决定了合规篇的必要性）**：EU AI Act Art.50 透明度义务 **2026-08-02 生效**；中国 GB 45438-2025《AI 生成合成内容标识》**2025-09-01 生效**；加州 SB 942 同日施行；C2PA 已成 ISO/IEC 22144。截至审计日，这些**都已是现行法规**，不是前瞻话题。

---

## 四、新架构

### 4.1 十六篇 / 七阶段

```
一 · 基础层        00 导论全景 · 01 数学与神经网络基础 · 02 序列建模与表示
二 · 架构核心      03 Transformer 架构核心 · 04 现代架构演进
三 · 训练全流程    05 GPU 与算子系统★ · 06 预训练 · 07 后训练与对齐
四 · 推理与部署    08 推理与推理时扩展 · 09 效率与部署
五 · 应用与智能体  10 检索、上下文与应用范式 · 11 Agent 智能体★
六 · 工程化与治理  12 工程化与运维★ · 13 评估与可解释性 · 14 安全、治理与合规
附录              15 动手实践★
```

★ = 全新篇。原「四 · 推理与应用」塞了 22 条（占 35%）明显超载，拆为三段。

### 4.2 关键新增

**Part 05 GPU 与算子系统**：`gpu-architecture` / `compute-budget`（含 Roofline）/ `triton-kernel`（含算子融合与编译）/ `flash-attention`

判定 FlashAttention 归属的依据 —— 它与 `attention-variants` 正交，不可合并：
- `attention-variants`（Part 04）解决**算什么**：MQA/GQA/MLA/NSA/SWA 是算法层的近似与稀疏
- `flash-attention`（Part 05）解决**怎么算**：IO 感知的精确重排，**数学输出完全一致、零近似**

**Part 12 工程化与运维**：`observability` / `prompt-eval-ci` / `cost-engineering` / `rag-in-production` / `guardrails`

这一层的核心命题是「**改一次 prompt 就是一次发布**」，以及「通用 APM 完全看不到这些失败」。

**Part 11 Agent 智能体**：`agent` / `tool-use-mcp` / `agent-memory` / `multi-agent` / `computer-use` / `agent-eval` / `agent-safety`

### 4.3 判定 dLLM 归属的通用判据

扩散语言模型放架构篇而非推理篇，依据是：

> **凡是权重层面不兼容的，就是架构/训练层，不是推理层。**

dLLM 改变的是似然分解方式本身，后果是三条权重级不兼容：权重不可复用（必须从头以掩码扩散目标预训练）、KV Cache 语义失效（无严格因果序）、困惑度不可与 AR 直接比。

若放推理篇与 `decoding` 并列，读者的直觉必然变成"扩散是第五种采样方式"——而这是读者自己永远发现不了的误读。

### 4.4 边界划分（防内容互相渗透）

重构后有四组条目存在渗透风险，各钉死一句分工线：

| 组 | 分工线 |
|---|---|
| 评估 vs 可观测性 | **离线比模型，线上比昨天** |
| 三条 RAG | **让它更准归 `advanced-rag`，让它跑三个月不烂掉归 `rag-in-production`** |
| 成本双条 | **效率篇讲怎么让它快，运维篇讲怎么让它不烧钱** |
| Agent 训 vs 搭 | **`agentic-rl` 讲怎么训一个 Agent，Agent 篇讲怎么搭一个 Agent** |

安全散在四处，各自声明不讲什么：

| 条目 | 边界 |
|---|---|
| `safety-alignment`(P07) | **训练侧**：对齐税、Constitutional AI、拒答校准。不讲攻击手法 |
| `agent-safety`(P11) | **执行侧**：间接提示注入、权限最小化、沙箱、不可逆操作的人类确认点 |
| `guardrails`(P12) | **运行时组件侧**：双向拦截的工程实现、误杀率与漏杀率调优 |
| `safety-redteam`(P14) | **攻方与测评侧**：越狱手法分类、自动化红队、防护栏的**评测**（不是实现） |

另有一条必写的警告：`structured-output` 须声明「**约束解码保证格式合法，不保证内容安全，它不是安全边界**」——很多人以为上了 JSON Schema 就防住了注入。

---

## 五、分批落地

### 5.1 三个 batch

新增 `batch` 字段，把「必备性」（priority）与「排期」（batch）两个维度分开——原方案 37 条 P0 里只有 20 条在首发批，P0 事实上被当两批标签在用。

**batch 1 · 首发 24 条**：一条从零到 RAG 的无断点认知闭环

```
what-is-llm → tech-map → model-families → language-model → tokenization
→ embeddings → attention✓ → multi-head → positional-encoding → architecture
→ kv-cache → moe✓ → objectives → sft → rlhf → peft-lora
→ decoding → prompt-engineering → structured-output → rag✓
→ hallucination → context-engineering → agent → tool-use-mcp
```

（✓ = 已完成，实际新写 21 条）

这条路径经过逐段走查，修掉了原方案的 5 个断点：
- 缺 `embeddings` — 刚学完切 token 直接进 QKV，且 RAG 的全部前提是"向量空间语义相近则距离相近"
- 缺 `positional-encoding` — 注意力置换不变，不讲位置编码，"完整架构"四个字是假的
- 缺 `structured-output` — 工具调用的技术底座
- 缺 `rlhf` — 否则读者形成"对齐 = SFT + LoRA"的错误认知
- 缺评估 — RAG 的存在理由就是抑制幻觉，讲完 RAG 不讲幻觉逻辑缺环

**batch 2 · 骨干补全**：含 LLMOps 整层、Part 05 系统篇、Agent 篇剩余、合规篇
**batch 3 · 前沿延伸**：P2 条目，v1.0 只给概要 + 精选外链

### 5.2 分批承诺（写进 site.policy）

> 目录立起来后长期停在占位状态，读者点开十次有九次是空页——**这对手册可信度的伤害大于目录不全**。

因此 v1.0 对外声明：P0/P1 写完整正文，P2 只提供概要与精选外链并标注「速览」。目录立 98 条，v1.0 实际写作量约 84 条，且每个点开的页面都有东西。

### 5.3 时效性标记

给 5 条加 `volatile: true`：`governance` / `data-copyright` / `watermark-provenance` / `model-families` / `serving-cost`。半衰期均在 6–12 个月，页面单独标注更新日期——**合规内容过期误导读者，后果比技术内容严重得多**。

---

## 六、落地操作

### 6.1 一条命令

```bash
# 预演（不改任何文件）
python tools/migrate_v02.py --dry-run

# 执行
python tools/migrate_v02.py

# 单独跑死链校验
python tools/migrate_v02.py --verify
```

脚本做六件事，其中后两件是手工操作容易漏的：

1. 全量备份 `pages/` + `toc.json` + 根级页面到 `.backup-<时间戳>/`
2. 精编页迁移（`rag.html` → `10-applications/`）
3. 清理旧骨架页与废弃篇目录（**精编页永不删除**，双重保险）
4. 切换 `toc.json`
5. 跑 `generate.py`
6. **重写残留硬编码链接**（index.html 25 处 + 精编页交叉引用）
7. 收尾**自动跑死链校验**，有残留即非零退出

### 6.2 为什么必须有第 6 步

Part 改号会导致 26 处死链，而 `generate.py` 不会管这些：

| 位置 | 问题 |
|---|---|
| `moe.html` → `../05-pretraining/distributed-training.html` | Part 改号后失效 |
| `rag.html` → `../06-posttraining/sft.html` | Part 改号后失效 |
| `index.html` 35 个硬编码链接 | 24 个需要更新 |
| `index.html` → `07-inference/ttt.html` | `ttt` 已被合并，需重定向到 `inference-scaling` |

脚本内置 `RETIRED_REDIRECT` 表处理退役条目的承接关系（这个关系无法自动推导，是编辑决策），未登记的退役条目会告警拦截。

### 6.3 受保护的 id（动了会断链）

以下 id 在重构中**必须沿用且不得跨篇移动**：

- `attention` / `moe` / `rag` — 三个精编页
- `attention-variants` — 被 `attention` 精编页硬引用
- `distributed-training` — 被 `moe` 精编页硬引用（因此拆分时让"并行策略"继承此 id）
- `sft` — 被 `rag` 精编页硬引用

---

## 七、人工待办（脚本处理不了）

`generate.py` 会跳过带 `handcrafted` 标记的精编页，以下两处需要手动补写：

1. **`pages/10-applications/rag.html`** — 加一节「嵌入模型与重排器选型」。原方案的 `embedding-models` 条目被砍后，其中"选型 / cross-encoder 重排 / 自建检索评测集"三块内容分流到此页（"对比学习训练嵌入模型"整体不收）。
2. **`pages/03-transformer/attention.html`** — 加一条指向 `flash-attention` 的引导。新建的 Part 05 不会让老页面自动知道它的存在，否则读者读完注意力原理，永远不知道还有"注意力为什么快"这一篇。

---

## 八、遗留风险

1. **总量与完成度的张力**。98 条 × 3000–5000 字 ≈ 30–50 万字。分批机制缓解了这个问题，但没有消除。若需进一步压缩，建议顺序：`privacy-security` 并入 `safety-redteam` → 可解释性两条合一 → 砍 `build-flashattention`（与 Part 05 重复度全书最高）。
2. **交叉引用写法**。本轮 Part 号变动两次，26 处死链就是第一次变动的产物。已要求所有 desc 内的交叉引用**一律用 topic id 而非 Part 号**，否则下次插篇会再来一轮。
3. **合规内容的维护**。五条 `volatile` 条目需要定期复核，尤其是各国立法的生效时间表。
4. **型号信息不入正文**。多篇 2026 年榜单彼此列的旗舰型号互相对不上（GPT-5.5 vs 5.6、Opus 5 vs Opus 4.8 并存），`model-families` 只写选型方法论与决策树，型号收敛到一个带日期戳的快照表并给持续评测方外链。

---

## 九、变更总览

| 维度 | v0.1 | v0.2 |
|---|---|---|
| 篇 / 条 | 11 / 62 | 16 / 98 |
| 阶段 | 4 | 7 |
| 原 id 沿用 | — | 56 条（仅 6 条因合并退役） |
| 新增字段 | — | `status` / `priority` / `batch` / `volatile` |
| 首发批 | 未定义 | 24 条无断点闭环 |

---

> 本方案由 AI 深度研究团队产出（调研 → 规划 → 审稿 → 修订 → 主编裁决五个环节）。所有引用来源建议在重要场景下二次核验时效性。
